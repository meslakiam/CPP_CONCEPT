#include <iostream>
#include <vector>
#include <algorithm>
#include <functional>
#include <ctime>

// int main( void )
// {
//     char        buffer[100];
// 	time_t      cur_time;
//     struct tm   *lt;

// 	cur_time = time(NULL);
//     lt = localtime(&cur_time);
    
//     strftime(buffer, sizeof(buffer), "%Y/%m/%d %H:%M:%S", lt);
//     std::cout  << buffer << std::endl;
// }